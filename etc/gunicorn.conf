CONFIG = {
    # 'mode': 'wsgi',
    #'working_dir': '/path/to/my/app',
    # 'python': '/usr/bin/python',
    'args': (
        
        '--bind=0.0.0.0:8080',
        '--workers=4',
        '--pythonpath=/home/box/web',
        'hello:simple_app',
    ),
}
